/**
 * Main view
 *
 * Copyright 2014 Joe Nudell
 *
 * @jsx React.DOM
 */

define([
    'react'
],
function(React) {

    var MainView = React.createClass({
        render: function() {
            return <div>Hello! React works, apparently.</div>
        }
    });

    return MainView;
});